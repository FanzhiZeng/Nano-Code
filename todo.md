# TODO

## 已完成

- [x] 搭建基于 `Anthropic` SDK 的最小命令行对话原型
- [x] 支持读取 `.env` 配置并发送多轮历史消息
- [x] 补充 `.env.example` 和基础项目说明
- [x] 完成启动配置校验与客户端初始化收口
- [x] 支持 `Ctrl+C` / `Ctrl+D` 优雅退出
- [x] 增加请求异常处理，避免直接崩溃
- [x] 增加日志文件输出，前台只保留关键信息
- [x] 跑通最小 `tool_use -> tool_result -> 再次推理` 闭环
- [x] 接入实验性的 `bash` 工具
- [x] 接入 `read_file` 工具
- [x] 支持“中间话术 -> 工具状态 -> 最终回答”的前台输出顺序
- [x] 提取 `tools/` 包，补齐工具抽象、注册表和统一执行入口
- [x] 提取 [agent_config.py](/home/luo/project/nano_code/agent_config.py) 作为配置数据结构模块

## P0: 可用性

- [x] 启动时校验 `ANTHROPIC_API_KEY`、`MODEL_ID`，缺失时给出明确报错
- [x] 兼容 `ANTHROPIC_BASE_URL` 为空的情况，避免未定义变量问题
- [x] 处理 `EOFError` 和 `KeyboardInterrupt`，支持优雅退出
- [x] 增加请求异常处理，避免网络或鉴权失败直接崩溃

## P1: Agent 基础能力

- [x] 设计消息循环，支持“模型输出 -> 工具执行 -> 结果回填 -> 再次推理”
- [x] 将当前硬编码工具整理为统一工具接口：`name`、`description`、`input_schema`、`handler`
- [x] 实现工具注册表，按工具名查找并执行
- [ ] 将工具执行从 `call_request()` 的专用逻辑提炼成通用分发逻辑
- [ ] 实现只读工具 `list_dir`
- [x] 实现只读工具 `read_file`
- [ ] 实现只读工具 `search_text`
- [x] 规范 `tool_use` 到 `tool_result` 的消息转换
- [x] 为工具执行补充基础日志，记录入参和结果摘要
- [ ] 为不同工具定义更细粒度的日志字段和错误分类
- [ ] 实现受控写入工具：编辑文件、创建文件
- [ ] 强化 shell 工具限制：工作目录、命令白名单/黑名单、输出截断策略

## P2: 上下文能力

- [ ] 启动时采集项目文件结构
- [ ] 根据用户问题挑选相关文件片段加入上下文
- [ ] 控制上下文长度，避免无上限追加历史

## P3: 工程化

- [ ] 将单文件脚本进一步拆分为 `config`、`client`、`agent` 模块
- [x] 将工具定义与工具执行器从主入口中拆分出去
- [ ] 细化日志输出，区分应用事件、模型请求、工具执行
- [ ] 为配置加载和消息循环补充基础测试
- [ ] 为工具注册和工具执行补充基础测试
- [ ] 补充开发说明和调试方法
