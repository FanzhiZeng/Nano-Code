# TODO

## 已完成

- [x] 搭建基于 `Anthropic` SDK 的最小命令行对话原型
- [x] 支持读取 `.env` 配置并发送多轮历史消息
- [x] 补充 `.env.example` 和基础项目说明
- [x] 完成启动配置校验与客户端初始化收口
- [x] 支持 `Ctrl+C` / `Ctrl+D` 优雅退出
- [x] 增加请求异常处理，避免直接崩溃
- [x] 增加日志文件输出，前台只保留关键信息
- [x] 跑通最小 `tool_use -> tool_result -> 再次推理` 闭环
- [x] 接入 `bash` 工具
- [x] 接入 `read_file` 工具
- [x] 接入 `write_file` 工具
- [x] 接入 `web_search` 工具
- [x] 实现 `TodoManager`，支持新增、更新、删除、筛选和文本渲染
- [x] 引入 `AgentSession` / `AgentRuntime`，分离会话状态和运行时依赖
- [x] 改造工具签名，使工具可访问当前 runtime / session
- [x] 接入 `todo` 工具，支持 `add` / `update` / `remove` / `read`
- [x] 在 system prompt 中注入当前 todo 摘要
- [x] 要求每次收到新任务时先规划 todo 再执行
- [x] 为所有工具补充统一的 schema `description`
- [x] 提取 `tools/` 包，补齐工具抽象、注册表和统一执行入口
- [x] 规范 `tool_use` 到 `tool_result` 的消息转换
- [x] 支持“中间输出 -> 工具状态 -> 最终回答”的前台展示顺序
- [x] 为工具执行补充基础日志，记录入参和结果摘要
- [x] 提取 [agent_config.py](/home/luo/project/nano_code/agent_config.py) 作为配置数据结构模块

## P0: 稳定性与安全

- [ ] 强化 `bash` 工具限制：工作目录、命令白名单/黑名单、输出截断策略
- [ ] 为 `web_search`、`bash` 等外部依赖工具补细粒度错误分类
- [ ] 为 `write_file` 增加更受控的写入策略，避免整文件覆盖成为唯一手段
- [ ] 统一工具层的返回格式，区分成功、失败和摘要字段

## P1: Agent 基础能力

- [x] 完成 `TodoManager`，实现任务规划的状态承载
- [x] 提供 `todo` 工具，让模型显式读写当前任务列表
- [ ] 观察“先规划后执行”提示词在真实会话中的稳定性
- [ ] 为不同工具定义更贴近业务的日志字段
- [ ] 支持在一轮内更好地展示多个工具调用的执行摘要

## P2: 上下文能力

- [ ] 启动时采集项目文件结构
- [ ] 根据用户问题挑选相关文件片段加入上下文
- [ ] 控制上下文长度，避免无上限追加历史和 todo 摘要膨胀

## P3: 工程化

- [ ] 将入口脚本进一步拆分为 `config`、`client`、`agent` 模块
- [ ] 为配置加载和消息循环补充基础测试
- [ ] 为工具注册和工具执行补充基础测试
- [ ] 为 `todo` 工具和 `TodoManager` 补充基础测试
- [ ] 细化日志输出，区分应用事件、模型请求、工具执行
- [ ] 补充开发说明和调试方法

## P4: 写能力演进

- [ ] 提供创建文件、局部替换、追加写入等更细的文件编辑能力
- [ ] 为文件修改加入预览、确认或最小变更策略
- [ ] 补充更适合代码编辑的工具，而不是依赖整文件重写
