# TODO

## 已完成

- [x] 搭建基于 `Anthropic` SDK 的最小命令行对话原型
- [x] 支持读取 `.env` 配置并发送多轮历史消息
- [x] 补充 `.env.example` 和基础项目说明

## P0: 可用性

- [ ] 启动时校验 `ANTHROPIC_API_KEY`、`MODEL_ID`，缺失时给出明确报错
- [ ] 兼容 `ANTHROPIC_BASE_URL` 为空的情况，避免未定义变量问题
- [ ] 处理 `EOFError` 和 `KeyboardInterrupt`，支持优雅退出
- [ ] 增加请求异常处理，避免网络或鉴权失败直接崩溃

## P1: Agent 基础能力

- [ ] 设计消息循环，支持“模型输出 -> 工具执行 -> 结果回填 -> 再次推理”
- [ ] 抽象工具注册接口，便于后续增加 shell、文件读写、搜索工具
- [ ] 实现只读工具：读取文件、列目录、搜索文本
- [ ] 实现受控写入工具：编辑文件、创建文件
- [ ] 实现 shell 工具，并限制工作目录在项目内

## P2: 上下文能力

- [ ] 启动时采集项目文件结构
- [ ] 根据用户问题挑选相关文件片段加入上下文
- [ ] 控制上下文长度，避免无上限追加历史

## P3: 工程化

- [ ] 将单文件脚本拆分为 `config`、`client`、`agent`、`tools` 模块
- [ ] 增加日志输出，区分用户输入、模型响应、工具结果
- [ ] 为配置加载和消息循环补充基础测试
- [ ] 补充开发说明和调试方法
